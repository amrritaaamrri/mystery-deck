/**
 * Arcana Match design contract: the complete route is a Gilded Reliquary game table, not app chrome.
 */

import GameCanvas from "./components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
